defaults:
  - dataloader: default
  - model: default
  - trainer: default

hydra:
  run:
    dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
  sweep:
    dir: sweeps/${now:%Y-%m-%d}
    subdir: ${hydra.job.num}
